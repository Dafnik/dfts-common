name: release dfx-bootstrap-icons

on:
  push:
    tags:
      - 'dfx-bootstrap-icons*'

jobs:
  dfx-bootstrap-icons:
    uses: dafnik/dfts-common/.github/workflows/release.yml@main
    with:
      lib: dfx-bootstrap-icons
    secrets: inherit
