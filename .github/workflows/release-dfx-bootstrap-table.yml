name: release dfx-bootstrap-table

on:
  push:
    tags:
      - 'dfx-bootstrap-table*'

permissions:
  id-token: write # Required for OIDC
  contents: read

jobs:
  dfx-bootstrap-table:
    uses: dafnik/dfts-common/.github/workflows/release.yml@main
    with:
      lib: dfx-bootstrap-table
