name: release dfx-qrcode

on:
  push:
    tags:
      - 'dfx-qrcode*'

jobs:
  dfx-qrcode:
    uses: dafnik/dfts-common/.github/workflows/release.yml@main
    with:
      lib: dfx-qrcode
    secrets: inherit
