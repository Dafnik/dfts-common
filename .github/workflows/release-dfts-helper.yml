name: release $LIB

env:
  LIB: dfts-helper

on:
  push:
    tags:
      - '$LIB*'

jobs:
  release-dfts-helper:
    uses: dafnik/dfts-common/.github/workflows/release.yml@main
    with:
      lib: $LIB