name: release dfts-qrcode

on:
  push:
    tags:
      - 'dfts-qrcode*'

permissions:
  id-token: write # Required for OIDC
  contents: read

jobs:
  dfts-qrcode:
    uses: dafnik/dfts-common/.github/workflows/release.yml@main
    with:
      lib: dfts-qrcode
