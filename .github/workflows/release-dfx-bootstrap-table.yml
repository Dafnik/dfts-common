name: release dfx-bootstrap-table

on:
  push:
    tags:
      - 'dfx-bootstrap-table*'

permissions:
  id-token: write # Required for OIDC
  contents: read

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: setup node and pnpm
        uses: dafnik/setup-node-pnpm@v4
        with:
          install: true

      - name: test dfx-bootstrap-table
        run: pnpm exec nx test dfx-bootstrap-table --configuration=ci --coverageReporters=cobertura

      - name: setup coverage report
        uses: ./.github/actions/setup-coverage-report
        with:
          path: coverage/libs/dfx-bootstrap-table/cobertura-coverage.xml

  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: setup node and pnpm
        uses: dafnik/setup-node-pnpm@v4
        with:
          install: true

      - name: build dfx-bootstrap-table
        run: pnpm exec nx build dfx-bootstrap-table

      - name: zip dfx-bootstrap-table
        run: |
          cd ./dist/libs/
          zip -r "dfx-bootstrap-table.zip" ./dfx-bootstrap-table
          cd -

      - uses: actions/upload-artifact@v4
        name: create dfx-bootstrap-table artifact
        with:
          name: dfx-bootstrap-table
          path: ./dist/libs/dfx-bootstrap-table.zip
          retention-days: 1

  release-github:
    name: Release zip to github releases
    needs: [build, test]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - uses: actions/download-artifact@v5
        with:
          name: dfx-bootstrap-table
          path: ./dist

      - name: get dfx-bootstrap-table version
        id: get-version
        shell: bash
        run: echo "VERSION=$(node -p "require('./libs/dfx-bootstrap-table/package.json').version")" >> $GITHUB_OUTPUT

      - name: set to versioned zip dfx-bootstrap-table-v${{ steps.get-version.outputs.VERSION }}.zip
        shell: bash
        run: |
          mkdir releases
          cp "./dist/dfx-bootstrap-table.zip" "./releases/dfx-bootstrap-table-v${{ steps.get-version.outputs.VERSION }}.zip"

      - name: releases lookup
        run: |
          cd releases
          ls -la
          du -hs
          cd -

      - name: get only latest changelog
        run: |
          ./tools/scripts/extractLatestVersionChangelog.sh ./libs/dfx-bootstrap-table/CHANGELOG.md
          cat RELEASE_CHANGELOG.md

      - name: create dfx-bootstrap-table-v${{ steps.get-version.outputs.VERSION }} release
        uses: softprops/action-gh-release@v2.5.0
        with:
          body_path: ./RELEASE_CHANGELOG.md
          files: ./releases/dfx-bootstrap-table-v${{ steps.get-version.outputs.VERSION }}.zip

  release-npm:
    name: Release to npm registry
    needs: [build, test]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v6
        with:
          registry-url: 'https://registry.npmjs.org'

      - uses: actions/download-artifact@v5
        with:
          name: dfx-bootstrap-table
          path: ./dist

      - name: unzip
        shell: bash
        run: |
          cd dist
          unzip dfx-bootstrap-table.zip
          cd -

      - name: dist lookup
        shell: bash
        run: |
          cd dist/dfx-bootstrap-table
          ls -la
          du -hs
          cd -

      - name: publish to npm registry
        env:
          NPM_CONFIG_PROVENANCE: true
        run: |
          cd dist/dfx-bootstrap-table/
          npm publish
